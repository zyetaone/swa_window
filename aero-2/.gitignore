node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Cesium runtime assets (copied from node_modules by scripts/sync-cesium.mjs)
static/cesiumStatic/

# Tile packs — hundreds of MB, rebuilt by tools/download-tiles.ts + pack-pmtiles.ts.
# Never commit these; the Pi gets them out of band, not through git.
/data

# Python bytecode — a survey script imported tools/*.py and this got swept in.
__pycache__/
*.pyc
